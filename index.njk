---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

{% set maxPosts = collections.test.length | min(3) %}
<h1>Latest {% if maxPosts == 1 %}test{% else %}{{ maxPosts }} test{% endif %}</h1>

{% set postslist = collections.test | head(-3) %}
{% set postslistCounter = collections.test | length %}
{% include "postslist.njk" %}

<p>More posts can be found in <a href="{{ '/tests/' | url }}">the archive</a>.</p>
